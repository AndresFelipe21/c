#include <stdio.h>

int main()
{
    int arr[50];
    int n,i ;
    
    printf("Enter the size of array:n");
    scanf("%d", &n);
    
    printf("Enter the elements of array:n");
    
    for(i=0;i<n;i++)
    
    {
        scanf("%d",&arr[i]);
    }
    
    return 0;
}
